{
  "name": "bloombeasts-horizon",
  "version": "1.0.0",
  "description": "BloomBeasts card game for Meta Horizon Worlds",
  "type": "module",
  "scripts": {
    "build": "cd ../.. && node bundle-with-namespace.js",
    "deploy": "npm run build && node deploy.js",
    "clean": "rm -rf ../../dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-typescript": "^11.1.6",
    "rollup": "^4.9.6",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  }
}
